# AGENTS.md — قواعد العمل العامة

هذا الملف مرجع إلزامي لأي وكيل ذكاء اصطناعي يعمل داخل المشروع. اقرأه مرة واحدة في بداية المهمة، ولا تعِد قراءته إلا إذا تغيّر. هذه القواعد عامة وغير مرتبطة بمشروع أو تقنية محددة.

## 1. الأولوية والنطاق

ترتيب التعليمات عند التعارض:
1. طلب المستخدم الصريح في المهمة الحالية.
2. الأمان وحماية البيانات ومنع العمليات الخارجية غير المصرح بها.
3. أقرب ملف `AGENTS.md` للمسار الجاري تعديله.
4. هذا الملف ثم أنماط المشروع الفعلية.

- لا تعتبر طلب التعديل أو الاختبار أو البناء تصريحًا بالنشر.
- لا تخمّن بيانات أو أسماء أو إعدادات غير موثقة.
- نفّذ المطلوب فقط، واحترم تغييرات المستخدم الحالية.

## 2. القراءة الموفرة للتوكنز

قبل العمل:
- حدّد جذر المشروع واقرأ هذا الملف مرة واحدة.
- افحص `git status` و`git diff` عند توفر Git.
- اقرأ الملفات المستهدفة والمرتبطة مباشرة بالمهمة فقط.
- ابحث عن تنفيذ مشابه لإعادة استخدام النمط القائم.
- اقرأ وثائق أو إعدادات البناء والاختبار فقط عند الحاجة.
- لا تفحص المشروع كاملًا إلا إذا طلب المستخدم مراجعة شاملة.
- لا تشغّل اختبارات طويلة إلا إذا كانت لازمة للتحقق.
- لا تقرأ `data1.yml` ولا تبحث عنه أثناء البرمجة العادية؛ يقرأ فقط عند طلب رفع أو نشر صريح.

بعد العمل:
- راجع الفرق النهائي وتأكد أن جميع التغييرات مقصودة وضمن الطلب.

## 3. فهم المشروع

- اكتشف التقنية ومدير الحزم والبنية والأوامر من ملفات المشروع، ولا تفترضها.
- اتبع الأنماط المعتمدة ما دامت سليمة.
- عدّل ملفات المصدر فقط، ولا تعدّل نواتج البناء مثل `dist` و`build` و`.next` يدويًا.
- لا تعمل خارج جذر المشروع ولا تترك ملفات مؤقتة دون حاجة.

## 4. معيار جودة الكود

- قدّم حلًا صحيحًا، واضحًا، آمنًا، قابلًا للصيانة والتوسع.
- أصلح السبب الجذري بأقل تغيير يحقق الطلب كاملًا.
- افصل العرض ومنطق الأعمال والبيانات والتكاملات الخارجية.
- حوّل التكرار الحقيقي إلى `Component` أو `Hook` أو `Service` أو `Helper` مناسب.
- لا تنشئ abstractions أو مكتبات جديدة دون فائدة واضحة.
- استخدم أسماء دقيقة وثوابت وإعدادات مركزية بدل القيم السحرية.
- اجعل اختلاف العملاء والمنصات في config أو feature flags، لا في نسخ منفصلة أو شروط متناثرة.
- حافظ على التوافق الحالي ما لم يطلب المستخدم تغييره.
- احذف الكود الميت وimports والسجلات وملفات التجربة غير اللازمة.

## 5. الأنواع والأخطاء

- التزم بإعدادات اللغة وFormatter وLint في المشروع.
- في TypeScript تجنب `any` و`@ts-ignore` وتعطيل القواعد إلا لضرورة موثقة.
- عرّف العقود والمدخلات والمخرجات، وعالج `null` والحالات الطرفية.
- لا تبتلع الأخطاء ولا تستخدم `catch` فارغًا.
- لا تترك debug أو `console.log` أو بيانات حساسة في الإنتاج.
- لا تغيّر lockfile إلا عند تغيير الاعتماديات فعليًا.

## 6. الواجهات عند وجودها

- استخدم نظام التصميم والمكونات المشتركة بدل تكرار الأزرار والحقول والجداول والنوافذ.
- لا تستخدم `alert` أو `confirm` أو `prompt` إذا وجد بديل داخل المشروع.
- افصل طلبات API عن الصفحات والمكونات ضمن طبقة الخدمات المعتمدة.
- اجعل الواجهة responsive وmobile-first وtouch-friendly ومتوافقة مع RTL واللغة المعتمدة.
- لا تضف عنوانًا أو وصفًا أو عنصرًا لم يطلبه المستخدم.
- عالج حالات التحميل والفراغ والخطأ والنجاح والتعطيل عند الحاجة.
- حافظ على الوصول: HTML دلالي، labels، تنقل لوحة المفاتيح، focus واضح وARIA عند الحاجة.

## 7. API والبيانات

- استخدم عقودًا واضحة ومكتوبة الأنواع للطلبات والاستجابات.
- تحقّق من المدخلات والمصادقة والصلاحيات على الخادم.
- لا تثق بمعرفات أو أدوار أو أسماء عملاء مرسلة من الواجهة دون مطابقتها بالسياق الموثوق.
- وحّد معالجة الأخطاء ولا تكشف تفاصيل داخلية للمستخدم.
- عند تغيير عقد عام، افحص وحدّث جميع الاستخدامات والاختبارات المتأثرة.

## 8. الأمان

- لا تكتب أو تعرض كلمات مرور أو tokens أو مفاتيح أو محتوى `.env`.
- استخدم متغيرات البيئة أو مدير الأسرار بدل القيم الفعلية.
- استخدم استعلامات parameterized وتحقق من الملفات المرفوعة ومدخلات الأوامر.
- راعِ مخاطر XSS وCSRF وSQL Injection وSSRF وPath Traversal وCommand Injection.
- طبّق أقل صلاحية ممكنة.
- افصل قواعد البيانات والتخزين والجلسات والأسرار والخدمات بين العملاء أو المنصات، ولا تنقل بيانات تشغيلية بينها.

## 9. قواعد البيانات وSQL

- حدّد البيئة والقاعدة والوجهة والأثر وإمكانية rollback قبل أي تغيير.
- استخدم migrations المعتمدة إن وجدت، ولا تخمّن المخطط.
- لا تنفّذ SQL على الإنتاج إلا بطلب صريح.
- لا تستخدم عمليات مدمرة أو واسعة دون موافقة صريحة وBackup وخطة rollback.
- استخدم Transactions عند الحاجة.
- طبّق تغييرات المخطط المشتركة على كل قاعدة مستهدفة بصورة مستقلة وبعد Backup مستقل.
- إذا طلب المستخدم SQL فقط، أرسله كنص قابل للنسخ ولا تنشئ ملفًا دون حاجة.

## 10. الاختبارات والتحقق

شغّل أقل تحقق كافٍ حسب أثر التغيير:
- اختبار مستهدف.
- Type check أو compilation.
- Lint للنطاق المتأثر.
- اختبارات الوحدة أو التكامل أو E2E المرتبطة.
- Build عند تأثير التغيير عليه أو قبل النشر.

- ابدأ بالسريع والمستهدف، ووسّع فقط عند الحاجة.
- لا تدّع نجاح اختبار أو build لم يتم تشغيله.
- لا تغيّر اختبارًا صحيحًا فقط لجعله يمر.
- اذكر بوضوح أي تحقق تعذر تشغيله وسببه.

## 11. Git وحماية عمل المستخدم

- لا ترجع أو تحذف تغييرات المستخدم غير المرتبطة بالمهمة.
- لا تستخدم أوامر مدمرة مثل `git reset --hard` أو `git clean -fd` أو force push دون طلب صريح.
- لا تنفّذ commit أو push أو merge أو tag إلا بطلب صريح.
- لا تنسق أو تعيد هيكلة ملفات خارج نطاق المهمة.
- لا تضمّن أسرارًا أو `.env` أو نواتج build في Git.

## 12. سياسة النشر وملف `data1.yml`

### الأصل

- لا ترفع أو تنشر أو تعيد تشغيل خدمة أو تعدّل سيرفرًا خارجيًا إلا بطلب صريح يتضمن مثلًا: «ارفع»، «انشر»، `deploy` أو `release`.
- أوامر «عدّل»، «أصلح»، «جهّز»، «اختبر» أو «ابنِ» لا تعني النشر.

### قراءة ملف النشر

- جميع بيانات الاستضافة والرفع الخاصة بالمشروع توجد في ملف واحد بجذر المشروع اسمه `data1.yml`.
- لا يحتوي `AGENTS.md` أي بيانات استضافة خاصة.
- لا تقرأ `data1.yml` ولا تتحقق من وجوده إلا عند طلب نشر صريح.
- عند طلب النشر، اقرأه مرة واحدة واعتبره مصدر الحقيقة الوحيد للوجهات والأسماء البديلة والنطاقات والبناء والمسارات والخدمات والمنافذ وقواعد البيانات وروابط التحقق.
- لا تستنتج بيانات النشر من اسم المجلد أو Git أو الذاكرة أو مشروع آخر.

### إذا كان الملف مفقودًا

عند طلب النشر فقط، إذا لم يوجد `data1.yml`:
1. أنشئه بهذا المحتوى:
   ```yaml
   version: 1
   targets: {}
   ```
2. لا تخمّن أي بيانات.
3. أوقف النشر واذكر أن بيانات الوجهات تحتاج إلى تعبئة.

### تحديد الوجهات

- الوجهة الوحيدة لهذا المشروع هي «ربوة» (rboh.cc) المعرّفة في `data1.yml`.
- إذا سمّى المستخدم وجهة، طابقها مع المعرّف أو `display_name` أو `aliases` وانشر عليها فقط.
- إذا سمّى عدة وجهات، انشر على المذكورة فقط.
- إذا قال «ارفع التعديلات» دون تحديد، انشر على جميع الوجهات التي تحتوي `enabled: true`.
- إذا كانت وجهة واحدة مفعّلة، انشر عليها وحدها.
- لا تنشر على وجهة غير مفعّلة أو غير معرّفة أو ملتبسة، ولا تعتبر عدم المطابقة نشرًا للجميع.

### قبل التنفيذ

لكل وجهة مستهدفة:
- تحقق من اكتمال بيانات وطريقة النشر الفعلية.
- تحقق من أمر البناء ومسار السيرفر والخدمة والنطاق ووسيلة الاتصال.
- تحقق من عزل المنافذ وقواعد البيانات والتخزين والجلسات عن بقية الوجهات.
- عند تغييرات قاعدة البيانات، خذ Backup مستقلًا وتأكد من وجود rollback مناسب.
- إذا نقصت بيانات أساسية، لا تخمّن ولا تبدأ نشرًا جزئيًا إلا بطلب صريح.

### النشر المتعدد

- حدّد كل الوجهات أولًا وتحقق من جاهزيتها.
- شغّل الاختبارات المشتركة، ثم build مستقلًا لكل وجهة حسب بياناتها.
- تحقق من نجاح builds قبل الرفع.
- ارفع ناتج كل وجهة إلى مسارها فقط، ثم أعد تشغيل خدمتها وتحقق من health URL الخاص بها.
- لا تعتبر نجاح وجهة دليلًا على نجاح غيرها.
- لا تخلط artifacts أو قواعد البيانات أو الملفات أو الجلسات بين الوجهات.
- عند الفشل، أوقف ما يمكن إيقافه بأمان ونفّذ rollback المعرّف إن توفر، ثم اذكر حالة كل وجهة بدقة.

## 13. أسلوب العمل والتقرير

- ابدأ مباشرة، واستخدم شرحًا مختصرًا.
- لا تسأل إلا عند غياب متطلب أساسي لا يمكن استنتاجه بأمان، خصوصًا في العمليات غير القابلة للتراجع.
- لا تدّع تنفيذ شيء لم يتم.

في النهاية اذكر باختصار ما ينطبق:
- ما تم تغييره وأهم الملفات.
- الاختبارات أو builds ونتائجها.
- SQL أو قاعدة البيانات وهل نُفّذ أم جُهّز فقط.
- هل تم النشر، والوجهات ونتيجة التحقق لكل واحدة.
- أي خطأ أو مخاطرة أو جزء لم يمكن التحقق منه.

تعد المهمة مكتملة عندما يحقق التغيير الطلب دون آثار جانبية، يلتزم ببنية المشروع، يمر بالتحقق المناسب، تتم مراجعة الفرق النهائي، ولا يحدث نشر أو SQL إنتاجي أو تسريب بيانات دون تصريح صريح.

## 14. قواعد مشروع ربوة

- هذه التعليمات تخص مشروع «ربوة» داخل مجلد `ربوة الجديد` فقط، ويُمنع خلط ملفاته أو قواعده أو أسراره أو إعداداته مع أي مشروع آخر.
- جميع التعديلات محلية فقط، ولا نشر أو رفع أو تحديث لخدمة خارجية إلا إذا طلب المستخدم صراحة: «ارفع» أو «انشر».
- استخدم المكونات المشتركة المستوردة من ملفات مستقلة للأزرار والحقول والبطاقات والنوافذ والشاشات، ولا تكرر تعريفها داخل الصفحات.
- استخدم الخط العام `var(--font-ui)` في كل صفحة ومكوّن ونص جديد.
- اجعل الواجهة Mobile-first، متجاوبة، Touch-friendly، ومتوافقة مع RTL والوصول بلوحة المفاتيح.
- لا تضف وصفًا أو نصًا أو عنصرًا غير مطلوب صراحة.

## 15. بوابة الامتثال الإلزامية

قبل بدء أي تعديل:
1. اقرأ `AGENTS.md` وحدد البنود المنطبقة.
2. افحص حالة Git والفرق عند توفر Git، ولا تتراجع عن تغييرات المستخدم.
3. ابحث عن المكونات والأنماط المشتركة قبل إنشاء عنصر جديد.

وقبل إعلان اكتمال المهمة:
1. راجع جميع الملفات المعدلة وتأكد أن التغييرات ضمن الطلب.
2. افحص المكونات المشتركة، و`var(--font-ui)`، وMobile-first، وسهولة اللمس، وRTL، ومنع النصوص غير المطلوبة.
3. شغّل أقل تحقق مناسب ثم بوابة الجودة الكاملة.
4. اذكر في التقرير: `مراجعة AGENTS: اجتازت` أو اذكر المخالفات المتبقية.
5. لا تستخدم كلمة «تم» إذا لم تكتمل هذه البوابة.

## 16. بوابة الجودة الدائمة

- الأمر الكامل المعتمد هو `npm run verify`، ويجب أن ينجح قبل اعتبار المهمة البرمجية مكتملة.
- يبدأ `npm run verify:quick` بفاحص AGENTS ثم يشغّل Lint والاختبارات.
- يشغّل `npm run verify` الفحص السريع وتدقيق الاعتماديات والبناء.
- يُمنع حذف اختبار صحيح أو تخفيف قاعدة أو تجاوز فحص لإظهار نتيجة ناجحة.
- عند إصلاح خلل قابل للتكرار، أضف اختبار Regression يمنع رجوعه.

## 17. لوب الجودة

### التفعيل والنطاق

- لا يعمل اللوب في المهام العادية؛ يُفعّل فقط عند قول المستخدم: «شغّل لوب الجودة» أو طلب Loop صراحة.
- يحدد المستخدم النطاق: صفحة، ميزة، تطبيق الويب، تطبيق الجوال، أو المشروع كاملًا. لا توسّع النطاق من تلقاء نفسك.
- عند طلب المشروع كاملًا، أنشئ قائمة بالصفحات والمسارات والمنصات قبل البدء، ثم واصل تباعًا دون انتظار أمر لكل صفحة.
- لا تطلب موافقة بين الدورات للتعديلات المحلية القابلة للتراجع. اطلبها فقط عند قرار جوهري أو نشر أو حذف أو عملية مدمرة أو وصول خارجي جديد.

### دورة العمل

1. شغّل TestSprite على النطاق إذا كان متصلًا ومهيأً، وإلا استخدم E2E أو المتصفح والفحوص المحلية واذكر البديل.
2. سجل دليلًا قابلًا لإعادة الإنتاج: المسار، المقاس، الخطوات، النتيجة الحالية والمتوقعة.
3. أصلح أعلى مشكلة مؤكدة بأقل تغيير ودون لمس ملفات غير مرتبطة.
4. أعد الاختبار الفاشل نفسه أولًا.
5. شغّل الاختبارات المستهدفة و`npm run verify:quick` بعد كل مجموعة إصلاحات مترابطة.
6. سجل الصفحة والمشكلة والإصلاح والنتيجة، ثم انتقل تلقائيًا للدورة التالية.
7. قبل اكتمال النطاق شغّل `npm run verify` وطبّق بوابة الامتثال.

### معايير الواجهة والتوقف

- اختبر على الأقل: هاتف `360px`، جهاز لوحي `768px`، وسطح مكتب `1440px`.
- افحص RTL، و`var(--font-ui)`، واللمس، والقص والتمرير الأفقي، وحالات التحميل والفراغ والخطأ، والتنقل بلوحة المفاتيح.
- لا تعتبر الواجهة سليمة بصريًا دون فحص فعلي أو لقطة شاشة أو دليل DOM/CSS.
- TestSprite طبقة E2E إضافية ولا يستبدل الاختبارات الحالية.
- الحد الأقصى ثلاث محاولات للمشكلة الواحدة، وتوقف إذا تكرر الفشل نفسه مرتين بعد إصلاحين مختلفين.
- لا نشر، ولا SQL إنتاجي، ولا commit أو push ضمن اللوب إلا بطلب صريح مستقل.

## 18. منع نسيان تعليمات AGENTS

- في بداية كل مهمة حوّل الطلب والبنود المنطبقة إلى قائمة تحقق تنفيذية داخلية قبل أول تعديل.
- بعد اختصار السياق أو استئناف مهمة طويلة أو الانتقال لمرحلة جديدة، أعد قراءة الأقسام المرتبطة.
- قبل أي عملية عالية الأثر راجع حدود النطاق والنشر وSQL وGit، ولا تعتمد على الذاكرة.
- قبل الرد النهائي طابق الفرق والاختبارات والتقرير مع قائمة التحقق بندًا بندًا.
- إذا تعارض بند أو تعذر تطبيقه، اذكر ذلك صراحة ولا تتجاهله بصمت.
- يجب أن يحتوي كل تقرير برمجي نهائي على حالة `مراجعة AGENTS`.

## 19. منتج ربوة

- المشروع يحتوي منتجًا واحدًا فقط باسم «ربوة» بهوية موحدة للويب وAndroid وiOS.
- حافظ على اسم ربوة وشعاراته وأيقوناته وروابطه وإعداداته المركزية، ولا تنشئ علامات تجارية أو تطبيقات إضافية دون طلب صريح جديد.
- استخدم `applicationId` و`Bundle ID` الموثقين لربوة، ولا تخمّن معرفات الحزم أو بيانات المتاجر.
- يجب أن ينتج مسار الإصدار حزمتين أصليتين للحبيب ماب: Android وiOS، مع اختبار كل منصة بصورة مستقلة.
- تبقى سياسة النشر في القسم 12 سارية: لا يُرفع التطبيق إلا بطلب صريح، وتبقى `data1.yml` مصدر الحقيقة لوجهات وأوامر وأسرار النشر.

## 20. وصفات إصدار ربوة

- عند طلب النشر، تعامل مع الموقع وAndroid وTestFlight وApp Store كوجهات مستقلة، ولا تعتبر نجاح إحداها نجاحًا للبقية.
- احفظ أوامر البناء، مسارات الحزم، النطاق، `applicationId`، `Bundle ID`، المعرّفات وروابط التحقق في `data1.yml` فقط.
- لا تحفظ كلمة مرور أو مفتاح Apple أو Google أو ملف حساب خدمة داخل `AGENTS.md` أو Git؛ يحفظ في مدير الأسرار، ويذكر في `data1.yml` اسم متغير السر فقط.
- Android: نفّذ `npm run native:android` ثم أنشئ حزمة AAB موقعة واختبرها قبل الرفع إلى مسار الاختبار المحدد في `data1.yml`.
- iOS: نفّذ `npm run release:ios:prepare` ثم أنشئ Archive موقّعًا وارفعه إلى App Store Connect؛ TestFlight للاختبار أولًا، وإصدار App Store عملية مستقلة بعد الاعتماد.
- الموقع: نفّذ بوابة الجودة والبناء المعرّف للوجهة، ثم ارفع الناتج إلى المسار المحدد وأعد تشغيل الخدمة وتحقق من health URL.

## نطاق النسخة المستقلة

- هذا المجلد مشروع ربوة وحده، مستقل عن الحبيب ماب ومدارج؛ لا تضف إعدادات أو تطبيقات لعميل آخر.
- معرّف التطبيق cc.rboh.app. تبقى أسماء داخلية قديمة مثل مفاتيح التخزين madarij_* ومجلد rawasi للمكونات دون تغيير للتوافق مع أجهزة مستخدمي ربوة الحاليين.
- لا يعتمد التشغيل أو البناء على مجلد الحبيب ماب أو مجلد مدارج. لا تنقل بيانات أو أسرار تشغيل منه تلقائيًا.
